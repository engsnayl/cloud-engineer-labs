apiVersion: v1
kind: Service
metadata:
  name: backend-api
spec:
  # Fault 1: Selector doesn't match pod labels
  selector:
    app: backend
    tier: api
  ports:
  # Fault 2: targetPort doesn't match containerPort
  - protocol: TCP
    port: 80
    targetPort: 8080
