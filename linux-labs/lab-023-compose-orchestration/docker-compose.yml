services:
  server:
    build: .
    container_name: lab023-compose-orchestration
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /opt/fullstack-app:/opt/fullstack-app
    stdin_open: true
    tty: true
