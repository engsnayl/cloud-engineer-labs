services:
  scripting:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lab028-scripting
    stdin_open: true
    tty: true
    volumes:
      - ./healthcheck.sh:/opt/scripts/healthcheck.sh
