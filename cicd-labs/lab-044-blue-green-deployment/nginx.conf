# BUG: Currently hardcoded to blue — no switching mechanism
upstream app {
    server app-blue:8080;
    # server app-green:8080;  # Uncomment to switch
}

server {
    listen 80;
    location / {
        proxy_pass http://app;
    }
}
