[
  {
    "id": "aws-003-mcq-1",
    "source_id": "aws-003",
    "domain": "aws",
    "subdomain": "networking",
    "priority": "critical",
    "difficulty": "easy",
    "question": "An EC2 instance has a Security Group that allows inbound TCP/443 from 0.0.0.0/0. The NACL on its subnet allows inbound TCP/443 from 0.0.0.0/0 but has no outbound rules other than the default explicit deny. A client opens an HTTPS connection. What happens?",
    "options": {
      "correct": "The SYN-ACK reply is dropped on egress at the NACL \u2014 NACLs are stateless and need an explicit outbound rule on ephemeral ports",
      "distractor_1": "The connection succeeds because the Security Group is stateful and automatically permits the corresponding outbound return traffic at both the SG and NACL layers",
      "distractor_2": "The inbound SYN is dropped at the subnet because NACLs require an outbound rule before evaluating any inbound traffic",
      "distractor_3": "The connection succeeds because AWS implicitly allows ephemeral-port return traffic at the subnet level once an inbound rule has matched"
    },
    "explanation": "NACLs evaluate each direction independently and have no notion of established connections \u2014 return traffic on ephemeral ports must be explicitly allowed outbound. The SG (stateful) does permit the reply, but the NACL drops it first. Distractor 1 misattributes the SG's statefulness to the NACL. Distractor 2 inverts the failure direction \u2014 NACLs evaluate inbound and outbound separately, so inbound succeeds and outbound is where this connection actually dies. Distractor 3 invents an implicit-allow that doesn't exist; NACLs default to explicit deny on anything not matched.",
    "bank": 1
  },
  {
    "id": "tf-002-mcq-1",
    "source_id": "tf-002",
    "domain": "terraform",
    "subdomain": "state",
    "priority": "critical",
    "difficulty": "medium",
    "question": "Two engineers run terraform apply simultaneously against the same S3 remote backend. State locking via DynamoDB is configured. What happens?",
    "options": {
      "correct": "The second apply fails immediately with a state lock error",
      "distractor_1": "Both applies succeed; Terraform merges the resulting state files",
      "distractor_2": "The second apply waits for the first to complete, then proceeds",
      "distractor_3": "Both applies succeed but the second silently overwrites the first's state"
    },
    "explanation": "DynamoDB state locking is advisory and fail-fast, not a queue. The second apply attempts to acquire the lock, sees it's held, and exits with an error. Distractor 1: Terraform never merges state files \u2014 that's a deliberate design choice. Distractor 2: it doesn't queue (some CI systems do, Terraform doesn't). Distractor 3 is literally what happens WITHOUT locking \u2014 the question is testing what locking actually prevents.",
    "bank": 1
  },
  {
    "id": "k8s-032-mcq-2",
    "source_id": "k8s-032",
    "domain": "kubernetes",
    "subdomain": "security",
    "priority": "high",
    "difficulty": "medium",
    "question": "You bind a ClusterRole to a ServiceAccount using a RoleBinding (not a ClusterRoleBinding). What permissions does the ServiceAccount receive?",
    "options": {
      "correct": "The ClusterRole's verbs and resources are granted only within the RoleBinding's namespace \u2014 even though the role itself is cluster-scoped",
      "distractor_1": "The ClusterRole's permissions are granted cluster-wide because the role is cluster-scoped, regardless of the binding type",
      "distractor_2": "The combination is invalid and rejected at apply time; you must use ClusterRoleBinding to reference a ClusterRole",
      "distractor_3": "The ServiceAccount receives no permissions because RoleBindings can only reference Roles, not ClusterRoles"
    },
    "explanation": "A RoleBinding referencing a ClusterRole grants those permissions scoped to the RoleBinding's namespace \u2014 this is the standard pattern for reusing one ClusterRole across multiple namespaces without duplicating it. Distractor 1 ignores the binding type and incorrectly grants cluster-wide. Distractor 2 invents an invalidity that doesn't exist. Distractor 3 inverts the truth \u2014 RoleBindings CAN reference ClusterRoles, that's the point. This pattern is heavily used to define one cluster-wide 'developer' role and bind it per-namespace.",
    "bank": 2
  },
  {
    "id": "docker-005-mcq-1",
    "source_id": "docker-005",
    "domain": "docker",
    "subdomain": "images",
    "priority": "medium",
    "difficulty": "medium",
    "question": "You change a single line in your application's source code. Which Dockerfile instructions are guaranteed to be re-executed (not cached)?",
    "options": {
      "correct": "The first COPY/ADD that includes the changed file, plus every instruction after it in the Dockerfile",
      "distractor_1": "Only the specific RUN commands that reference the changed file in their command-line invocation",
      "distractor_2": "Every instruction in the Dockerfile, because any source change invalidates the entire build for safety",
      "distractor_3": "Only the FROM and CMD instructions; intermediate instructions remain cached as long as they don't reference the changed file directly"
    },
    "explanation": "Cache invalidation in Dockerfile builds is positional: the moment a layer is invalidated, everything after it is re-run too. So COPY changing busts that COPY layer and every subsequent one. Distractor 1 (only specific RUNs) misses cascade. Distractor 2 (everything) over-states \u2014 earlier layers (FROM, RUN apt-get) stay cached if their inputs didn't change. Distractor 3 inverts the model \u2014 intermediate instructions are exactly the ones that re-run.",
    "bank": 2
  },
  {
    "id": "cicd-050-mcq-1",
    "source_id": "cicd-050",
    "domain": "cicd",
    "subdomain": "operations",
    "priority": "critical",
    "difficulty": "medium",
    "question": "A pipeline stage fails intermittently \u2014 about 1 in 5 runs. The team's first impulse is to add `retry: 3` and move on. Why is that a bad first move?",
    "options": {
      "correct": "Retry hides the cause and erodes signal \u2014 the next time the underlying issue gets worse, the team has no historical data to debug",
      "distractor_1": "Retrying causes GitHub Actions to charge double per attempted run, doubling the CI bill within weeks",
      "distractor_2": "Retry attempts run in a different runner environment, so a flake on one runner won't reproduce on the retry \u2014 making the issue impossible to debug",
      "distractor_3": "Retry blocks downstream stages from starting until all retry attempts complete, slowing every successful run by 3x"
    },
    "explanation": "Flaky tests / pipelines have causes (timing, parallel state, dependency networks, runner variance). Retrying is a coping mechanism, not a fix \u2014 and once retries are in place, the team loses motivation to diagnose. Distractor 1 (double billing) over-states; retries cost something but not double. Distractor 2 inverts environment behaviour; fresh runners reset state, which sometimes hides a flake (helpful for shipping, bad for debugging). Distractor 3 ignores that retry only runs on failure, not on success.",
    "bank": 2
  },
  {
    "id": "git-006-mcq-1",
    "source_id": "git-006",
    "domain": "git",
    "subdomain": "workflow",
    "priority": "medium",
    "difficulty": "easy",
    "question": "You add `secrets.json` to `.gitignore` AFTER having already committed and pushed the file. What happens?",
    "options": {
      "correct": ".gitignore only affects untracked files; secrets.json is already tracked, so it stays in the repo and continues to be committed on changes",
      "distractor_1": "Git automatically retroactively removes the file from history once it's added to .gitignore",
      "distractor_2": "The next push fails because Git refuses to push a file that's now in .gitignore until you delete it from disk",
      "distractor_3": "Future commits will exclude the file silently; the historical commits remain but new versions stop being tracked"
    },
    "explanation": "`.gitignore` is for UNTRACKED files. Once a file is tracked, `.gitignore` is ignored for it. You need `git rm --cached secrets.json` to stop tracking, then commit. Distractor 1 invents retroactive removal. Distractor 2 invents a push failure. Distractor 3 invents auto-exclusion of future modifications; Git keeps tracking until you explicitly stop it. This is the #1 gotcha behind 'I gitignored the file, why is it still appearing in PRs?'",
    "bank": 2
  }
]